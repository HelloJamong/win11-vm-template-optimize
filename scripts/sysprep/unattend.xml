<?xml version="1.0" encoding="utf-8"?>
<!--
  ======================================================================
  Windows 11 VM 마스터 템플릿 Sysprep 응답 파일
  ======================================================================
  아키텍처:
    - 프로필 위치: C:\Users 유지 (이동 금지)
    - 사용자 데이터: first_logon.ps1 이 D:\UserData\{사용자명} 으로 리디렉션
    - C 드라이브: VirtualBox 스냅샷 대상
    - D 드라이브: VirtualBox Writethrough 설정 (스냅샷 제외)

  사용 방법:
    1. 이 파일을 C:\Windows\System32\Sysprep\unattend.xml 로 복사
    2. first_logon.ps1 을 C:\Windows\Setup\Scripts\first_logon.ps1 로 복사
    3. sysprep.exe /generalize /oobe /shutdown /unattend:C:\Windows\System32\Sysprep\unattend.xml

  설계 근거:
    - ProfilesDirectory 이동 방식은 VHD 스냅샷 구조, Sysprep, Store 앱과 충돌
    - CopyProfile=true 로 현재 사용자 설정을 Default Profile 로 보존
    - 쉘 폴더 리디렉션(D:\UserData)은 first_logon.ps1 에서 신규 사용자마다 적용
  ======================================================================
-->
<unattend xmlns="urn:schemas-microsoft-com:unattend"
          xmlns:wcm="http://schemas.microsoft.com/WMIConfig/2002/State">

  <!--
    generalize 패스
    sysprep /generalize 실행 시 처리됩니다.
  -->
  <settings pass="generalize">
    <component name="Microsoft-Windows-Shell-Setup"
               processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35"
               language="neutral"
               versionScope="nonSxS">
      <!--
        CopyProfile=true
        Audit Mode 현재 사용자(Administrator)의 HKCU 설정 및 프로필 파일을
        Default User Profile 로 복사합니다.
        이후 OOBE 를 통해 생성되는 모든 신규 사용자는 이 Default Profile 을 기반으로 시작합니다.

        주의: CopyProfile 이 복사하지 않는 항목(Default Apps, 쉘 폴더 리디렉션 등)은
        first_logon.ps1 에서 재적용합니다.
      -->
      <CopyProfile>true</CopyProfile>
    </component>
  </settings>

  <!--
    oobeSystem 패스
    OOBE 완료 후 처리됩니다.
  -->
  <settings pass="oobeSystem">
    <component name="Microsoft-Windows-Shell-Setup"
               processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35"
               language="neutral"
               versionScope="nonSxS">

      <TimeZone>Korea Standard Time</TimeZone>

      <OOBE>
        <!-- VM 템플릿 환경에서 불필요한 OOBE 화면을 최소화합니다 -->
        <HideEULAPage>true</HideEULAPage>
        <HideOEMRegistrationScreen>true</HideOEMRegistrationScreen>
        <HideOnlineAccountScreens>true</HideOnlineAccountScreens>
        <HideWirelessSetupInOOBE>true</HideWirelessSetupInOOBE>
        <!-- 3 = Windows 기본 보호 설정 적용 (자동) -->
        <ProtectYourPC>3</ProtectYourPC>
      </OOBE>

      <!--
        FirstLogonCommands: 최초 사용자 로그인 시 자동 실행
        실행 순서: Order 값이 낮을수록 먼저 실행됩니다.

        first_logon.ps1 수행 내용:
          1. D:\UserData\{사용자명} 폴더 구조 생성
          2. 쉘 폴더(Desktop/Documents/Downloads/Pictures/Videos/Music) D 드라이브로 리디렉션
          3. Appx 불필요 앱 제거 (Xbox/PhoneLink/Copilot/Teams/Clipchamp)
          4. Provisioned Appx 제거 (신규 사용자 자동 설치 방지)
          5. HKCU 설정 재적용 (Search/Copilot/ConsumerExperience/Telemetry/Explorer)
          6. Explorer 재시작
          7. 재실행 방지 플래그 설정
      -->
      <FirstLogonCommands>
        <SynchronousCommand wcm:action="add">
          <Order>1</Order>
          <CommandLine>powershell.exe -NoProfile -ExecutionPolicy Bypass -File "C:\Windows\Setup\Scripts\first_logon.ps1"</CommandLine>
          <Description>VM 템플릿 최초 로그인 초기화</Description>
          <RequiresUserInput>false</RequiresUserInput>
        </SynchronousCommand>
      </FirstLogonCommands>

    </component>
  </settings>

</unattend>
